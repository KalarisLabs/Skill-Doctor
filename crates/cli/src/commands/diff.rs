//! The `diff` command — diffing two scan results.

use anyhow::Result;

pub async fn run(_v1: &str, _v2: &str) -> Result<()> {
    println!("Diff command is not fully implemented yet.");
    Ok(())
}
