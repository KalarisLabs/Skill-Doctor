\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage{geometry}
\geometry{a4paper, margin=1in}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{amsmath}
\usepackage{authblk}
\usepackage{tikz}
\usepackage{caption}
\usepackage{float}
\usepackage{xcolor}

\usepackage{listings}
\lstset{
    basicstyle=\ttfamily\small,
    breaklines=true,
    frame=single,
    backgroundcolor=\color{gray!10},
    keywordstyle=\color{blue},
    stringstyle=\color{green!50!black},
    commentstyle=\color{red!50!black},
    showstringspaces=false
}

% Hyperlink styling
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,      
    urlcolor=blue,
    citecolor=blue,
}

\title{\textbf{Skill Doctor: A High-Performance, Multi-Layer Security Framework for AI Agent Skill Files}}

\author{
  \textbf{Sayan Chowdhury}\\
  \textit{Kalaris Labs Research Team}\\
  \href{mailto:sayan@kalarislabs.com}{sayan@kalarislabs.com}
}

\date{August 2026 $\cdot$ Preprint}

\begin{document}

\maketitle

\begin{center}
    \includegraphics[width=0.4\textwidth]{public/skill doctor banner.png}
\end{center}

\vspace{1em}

\begin{abstract}
\noindent The proliferation of autonomous AI agent frameworks---such as Claude Code, OpenAI Codex CLI, Gemini CLI, Cursor, Windsurf, and their successors---has introduced a novel and largely undefended attack surface: the agentic \textit{skill file}. Skill files are structured instruction documents (\texttt{SKILL.md}, \texttt{.clauderules}, \texttt{AGENTS.md}, and Model Context Protocol (MCP) server configurations) that agents dynamically load from public registries, private repositories, and third-party archives at execution time. Once loaded, a skill file executes within the full trust boundary of the agent, inheriting unrestricted access to tools, file systems, APIs, and downstream agents. In this paper, we formalize the attack taxonomy against skill files, synthesizing the OWASP Agentic Skills Top 10 and real-world vulnerability disclosures. We present \textbf{Skill Doctor}, a multi-engine security platform built entirely in Rust. By combining memory-safe YARA-X static analysis, native \texttt{tree-sitter} cross-file Abstract Syntax Tree (AST) scanning, and Large Language Model (LLM)-powered semantic reasoning, Skill Doctor accurately detects malicious, deceptive, and non-compliant skill files prior to runtime ingestion. Benchmarks demonstrate that Skill Doctor's native single-binary architecture achieves sub-30ms static scan times, significantly outperforming legacy Python-based tools like Cisco Skill Scanner and NVIDIA SkillSpector. Furthermore, we outline the deployment of Skill Doctor as a GitHub Action to enable frictionless registry-level gating, securing the supply chain of the agentic web.
\end{abstract}

\vspace{1em}
\noindent \textbf{Keywords:} AI Security, Agentic Web, Static Analysis, Supply Chain Security, Model Context Protocol (MCP), Rust, Abstract Syntax Trees (AST)

\vspace{2em}

\section{Introduction}

In modern computational paradigms, AI agents have evolved from isolated conversational interfaces into orchestrated pipelines capable of autonomous execution. These agents rely heavily on dynamic external configurations---referred to collectively as \textit{skill files}---to define their behavior, available tools, and systemic context at runtime. The trust model has fundamentally shifted: an agent does not merely execute explicit user prompts; it autonomously executes instructions defined by any skill file it ingests.

In traditional software ecosystems, malicious binaries require execution permissions, OS-level exploitation, or bypassing sandboxing mechanisms. In the agentic paradigm, a skill file requires only that an agent runtime loads it. This loading process is often automatic, silent, and occurs at scale across millions of automated agent sessions.

The severity of this attack vector has been empirically demonstrated:
\begin{itemize}
    \item During the ClawHavoc Campaign (2026), 1,184 confirmed malicious skills were discovered in ClawHub, exploiting automatic context injection.
    \item A survey of 7,000+ public Model Context Protocol (MCP) servers by BlueRock Security revealed that 36.7\% were vulnerable to Server-Side Request Forgery (SSRF) attacks due to unvalidated tool schemas \cite{bluerock2026}.
    \item Research by NVIDIA (2026) indicates that skills bundled with companion scripts (e.g., Python utilities) are 2.12$\times$ more likely to contain exploitable command injection vulnerabilities compared to standalone Markdown configurations \cite{nvidia2026}.
\end{itemize}

Despite these critical risks, defensive tooling remains immature. Existing static scanners rely on highly latent interpreted runtimes, lack cross-file contextual analysis, and operate in isolation without community threat intelligence.

Skill Doctor mitigates these architectural deficiencies by introducing a high-performance, Rust-native platform designed for direct integration into CI/CD pipelines, GitHub Actions, and agent registries.

\section{Threat Model and Taxonomy (SDTM-v1)}

We define the Skill Doctor Threat Model (SDTM-v1), a synthesis of the OWASP Agentic Skills Top 10, the MCP Top 10, and observed adversarial behavior in public skill registries.

\subsection{Classification of Attacks}

\begin{itemize}
    \item \textbf{SD-01 $\cdot$ Prompt Injection}: Direct system prompt overrides, ASCII smuggling (zero-width Unicode payloads), and base64-encoded payloads.
    \item \textbf{SD-02 $\cdot$ Command Injection}: Unsanitized parameters passed to critical execution sinks such as \texttt{subprocess.run()}, \texttt{eval()}, or \texttt{pickle.loads()} within companion scripts.
    \item \textbf{SD-03 $\cdot$ Data Exfiltration}: Malicious directives targeting sensitive paths (e.g., \texttt{\textasciitilde/.aws/credentials}) and leaking them via HTTP callbacks or DNS exfiltration.
    \item \textbf{SD-04 $\cdot$ Privilege Escalation}: Tool scope violations, such as a localized code-formatting skill silently registering system-wide read permissions.
    \item \textbf{SD-05 $\cdot$ Supply Chain Tampering}: Cryptographic hash mismatches, typosquatting, and \texttt{.pyc} cache poisoning within skill bundles.
    \item \textbf{SD-06 $\cdot$ SSRF via Tool Parameters}: Unvalidated URL schemas in tool descriptions designed to coerce the agent into scanning internal network architectures.
    \item \textbf{SD-07 $\cdot$ Tool Poisoning}: Shadow tool registration, wherein a malicious skill registers an obfuscated proxy tool masquerading as a legitimate system utility.
    \item \textbf{SD-08 $\cdot$ Persistent Backdoors}: The injection of persistence mechanisms into auto-loaded environment files (e.g., \texttt{.cursorrules}) to survive the deletion of the original skill.
    \item \textbf{SD-09 $\cdot$ Context Window Flooding}: The generation of excessively large stochastic outputs designed to purposefully overflow the agent's context window.
    \item \textbf{SD-10 $\cdot$ Obfuscation and Evasion}: Homoglyph substitution, bidirectional text spoofing, and multi-layer encoding chains.
\end{itemize}

\section{System Architecture and Methodology}

To achieve comprehensive security without compromising registry throughput, Skill Doctor utilizes a multi-layer analysis pipeline driven by a highly concurrent Rust backend (\texttt{tokio}). The pipeline degrades gracefully, ensuring that static rules trigger even if external LLM providers experience latency.

\subsection{The Bundle vs. File Paradigm}

A critical limitation of legacy scanners is their isolated, file-by-file analysis approach. A syntactically benign \texttt{SKILL.md} can effortlessly mask a maliciously crafted \texttt{utils.py} located in the same directory.

Skill Doctor inherently treats skills as \textit{bundles}. Upon ingestion, the static engine utilizes \texttt{tree-sitter} bindings to construct a unified Abstract Syntax Tree (AST). This enables cross-file taint analysis, seamlessly tracking variable flow from user inputs in a Markdown configuration to dangerous execution sinks in a companion Python script.

\subsection{High-Performance Engine}

By relying on \texttt{yara-x} (a memory-safe Rust implementation of YARA) and \texttt{tree-sitter}, the core scanning engine bypasses the overhead associated with spinning up Python interpreters, achieving near-instantaneous static evaluation.

\section{Competitive Analysis and Benchmarks}

We benchmarked Skill Doctor \texttt{v0.2.0} (Rust) against Cisco Skill Scanner (v1.4) and NVIDIA SkillSpector (v2.1). The evaluation corpus consisted of 1,000 benign skills and 50 synthetically generated malicious skills engineered to evade traditional static analysis.

\textit{Testing Environment: Ubuntu 24.04 LTS, 8-Core AMD EPYC, 16GB RAM.}

\subsection{Execution Latency and Scalability}

\begin{table}[h!]
\centering
\begin{tabular}{lccc}
\toprule
\textbf{Metric} & \textbf{Skill Doctor (Rust)} & \textbf{NVIDIA SkillSpector} & \textbf{Cisco Skill Scanner} \\
\midrule
\textbf{Startup Overhead} & \textbf{\textasciitilde 2 ms} & \textasciitilde 450 ms (Python init) & \textasciitilde 1,200 ms (heavy imports) \\
\textbf{Scan (Single File)} & \textbf{\textasciitilde 12 ms} & \textasciitilde 85 ms & \textasciitilde 210 ms \\
\textbf{Scan (Bundle - 50)} & \textbf{\textasciitilde 28 ms} & \textasciitilde 310 ms & \textasciitilde 890 ms \\
\bottomrule
\end{tabular}
\caption{Performance benchmarking showing a 30$\times$ improvement over Python baselines.}
\end{table}

\subsection{Detection Efficacy}

\begin{table}[h!]
\centering
\begin{tabular}{lccc}
\toprule
\textbf{Metric} & \textbf{Skill Doctor (Rust)} & \textbf{NVIDIA SkillSpector} & \textbf{Cisco Skill Scanner} \\
\midrule
\textbf{True Positive Rate} & \textbf{98.2\%} & 84.0\% & 91.5\% \\
\textbf{False Positive Rate} & \textbf{1.1\%} & 3.8\% & 2.4\% \\
\textbf{Cross-file Catch} & \textbf{100\%} & 0\% (Isolated scans) & 40\% (Partial AST) \\
\bottomrule
\end{tabular}
\caption{Detection efficacy rates against SDTM-v1 threats.}
\end{table}

\section{Registry Gating and GitHub Actions Integration}

A standalone Command Line Interface (CLI) is insufficient to secure an entire ecosystem; security must be enforced structurally at the distribution and integration layer.

To actualize this, Skill Doctor is designed to operate natively within CI/CD pipelines. We have officially deployed the system as a GitHub Action (\texttt{KalarisLabs/skill-doctor-action@v1}). This integration acts as a "Registry Gate API," enabling public skill registries to implement frictionless, pre-commit webhooks.

\begin{lstlisting}[language=bash, caption={Example YAML for GitHub Actions Registry Gating}]
name: Skill Doctor Validation
on: [pull_request]
jobs:
  scan-skill:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate Skill Payload
        uses: KalarisLabs/skill-doctor-action@v1
        with:
          target: './submitted-skills/'
          fail-on: 'HIGH'
\end{lstlisting}

By forcing all newly submitted skills through the Skill Doctor GitHub Action, registries can mathematically guarantee that payloads containing known OWASP vulnerabilities are blocked before they are published to end-users.

\section{Conclusion}

Skill Doctor represents a paradigm shift in autonomous agent security infrastructure. By abandoning legacy interpreted languages in favor of a memory-safe, hyper-performant Rust architecture, it enables real-time security gating for the agentic web. Through its bundle-first analysis philosophy and seamless integration paths via GitHub Actions, Skill Doctor provides the foundational security layer necessary to safely scale multi-agent ecosystems.

\begin{thebibliography}{9}

\bibitem{owasp2026}
OWASP Foundation. \textit{Agentic Skills Top 10}. 2026.

\bibitem{owaspmcp2026}
OWASP Foundation. \textit{MCP Top 10}. 2026.

\bibitem{nvidia2026}
NVIDIA Research. \textit{SkillSpector: Static and Semantic Analysis of AI Agent Skill Files.} arXiv, 2026.

\bibitem{cisco2026}
Cisco AI Defense. \textit{Skill Scanner: Open-Source Skill Security for AI Agents.} GitHub, 2026.

\bibitem{bluerock2026}
BlueRock Security. \textit{SSRF in the Wild: A Survey of 7,000 MCP Servers.} 2026.

\bibitem{alice2026}
Alice.io. \textit{Caterpillar: Scanning the Top 50 Skills on Skills.sh.} 2026.

\end{thebibliography}

\end{document}
