# Results Directory

This directory stores the output reports (e.g., SARIF, JSON, HTML) generated by the Skill Doctor CI pipeline when scanning the `tests/corpus/` dataset. These results are then picked up by the Kane CLI for TestMu AI visual validation.
